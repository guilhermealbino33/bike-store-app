{% comment %} <script src="{{ 'inventory-management-form.js' | asset_url }}" defer></script> {% endcomment %}
{% for variant in product.variants %}
    {% assign quantity = variant.inventory_quantity %}
{% endfor %}

Hello World! 
Quantity in stock: {{ quantity }}
<script>
    console.log("block.settings.product", {{ block.settings.product | json }})
</script>
{% schema %}
{
    "name": "Inventory Management",
    "templates": ["product"],
    "target": "section",
    "settings": [
        { "type": "product", "id": "product", "label": "product", "autofill": true }
    ]

}
{% endschema %}